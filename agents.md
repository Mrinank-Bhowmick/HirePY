# Agents Knowledge / App Metadata

* **UI Theme Details:** Light theme, utilizing vibrant `cyan` and `blue` extensively as core colors with soft, glassy backgrounds. Reverted from a monochromatic/slate approach back to high-impact gradients, striking buttons, and cyan accents for maximum visual appeal.
* **Component Modifications:**
    * `app/page.tsx`, `Footer.tsx`, `MeetingBox.tsx`, `HowItWorks.tsx` and others strictly use `cyan` and `blue` for primary glows, active states, text highlights, and animated elements rather than flat shades of slate.
